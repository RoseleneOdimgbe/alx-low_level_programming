#include <stdio.h>
#include <stdlib.h>

/**
 * add - this adds two integers
 * @a: this is the first integer
 * @b: this is the second integer
 * Return: addition
 */

int add(int a, int b)
{
	return (a + b);
}
